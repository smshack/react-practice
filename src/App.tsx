import { Button } from "@/components/ui/button"

function App() {
  return (
    <div className="h-screen flex items-center justify-center">
      <Button>
        로그인
      </Button>
    </div>
  )
}

export default App